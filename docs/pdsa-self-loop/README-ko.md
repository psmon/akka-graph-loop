# 자기개선 루프 — 이 프로젝트를 만든 22번의 PDSA 사이클

**[English](README.md) · 한국어**

이 프로젝트는 PDSA 도구를 *만들기만* 한 게 아니다. **그 도구로 만들어졌다.** 2026-08-26 이후의 모든
변경은 `pdsa` CLI 자신의 Plan → Do → Study → Act 루프를 거쳤고, 모든 단계가 지금도 그래프에 남아 있다.

이 문서는 그 기록을 루프 자신의 메모리에서 되읽어 쓴 것이다:

```bash
pdsa history --to 22 --full --project akka-graph-loop   # 전 회차 타임라인(오름차순)
pdsa show 21 --project akka-graph-loop                  # 한 회차 상세
```

> 이 두 명령은 이 문서를 시작할 때 **없었다.** 그래서 그래프 JSON을 손으로 파싱해야 했는데, 그것이야말로
> 루프가 드러내야 할 종류의 공백이라 [사이클 #22](#22--history--show--루프-자신의-과거를-조회한다)가 됐다.
> 이 문서는 자기가 설명하는 CLI만으로 만들어진 첫 산출물이다.

---

## 22회가 실제로 바꾼 것

| | 사이클 1 | 사이클 22 |
|---|---|---|
| 테스트 | — | **271개** |
| 루프가 자신을 판정할 수 있나? | `expected` 필드 자체가 없음 | 회차마다 `met / partial / unmet` |
| LLM 프로바이더 | 1개 (OpenAI API 키) | **5개** (API 키 · 키리스 로컬 · GPT OAuth · Codex · `claude -p`) |
| 에이전트가 읽을 수 있는 출력 | 프로즈뿐 | 9개 명령의 `--json` |
| LLM 호출 실패가 메모리를 오염시키나? | **그렇다 — 100%** | **아니다 — 실패 주입 20회에 0건** |
| 지난 회차를 조회할 수 있나? | 불가 | `pdsa show <n>` / `pdsa history` |

**기대 충족률: 판정된 18회 중 11회(61%).** 이 숫자는 추세를 오히려 축소해 보여준다:

```
사이클 4–14 (처음 판정된 10회)   ████░░░░░░   met 4회  →  40%
사이클 15–22 (마지막 8회)        ████████░░   met 7회  →  88%
```

루프가 **자기 자신을 예측하는 능력**이 좋아졌다는 뜻이고, 작업 전에 검증 가능한 기대 평가를 쓰는 이유가
바로 그것이다.

---

## 기록의 모양

세 회차는 **판정이 없다.** 데이터 누락이 아니라 그 자체가 역사다:

- **#1–#3**은 폐루프가 생기기 전이다. 네 단계를 기록할 수는 있었지만 무엇과 비교해야 할지가 없었다.
- **#11**은 유일하게 중도에 버려진 회차다. 계획만 있고 Study가 없다. 루프는 자기 미결을 조용히 지우지 않으므로
  지금도 그 상태로 그래프에 남아 있다.

그리고 **#12는 프로젝트 역사상 유일한 `unmet`**이다 — 계획은 조사 문서를 요구했는데 수행은 코드를 냈고,
Study가 그 어긋남을 성공으로 반올림하지 않고 잡아냈다.

다섯 회차는 **보강 사이클**(`REINFORCES` 엣지)이다. Act가 "이 갭은 아직 안 닫혔다"고 판단해 만들어진 것들:

```
#4 ← #5      #12 ← #13      #14 ← #15 ← #16      #20 ← #21
```

`#15(partial) → #16(met)`이 이 장치가 의도대로 작동한 사례다. #15는 기능이 된다고 주장했지만 전체 체인을
실증하지 않았고, #16은 오직 그것을 실증하기 위해 존재했다.

---

## 회차별 기록

각 카드는 그 회차가 무엇을 시도했고, 어떻게 판정됐고, 무엇을 가르쳤는지를 담는다.
(카드 이미지는 영문 공용 — 두 언어판이 같은 이미지를 쓴다.)

### 1기 · 기록만 — 아직 자신을 판정하지 못한다

#### #1 — CLI 기본기능 검증과 유닛테스트 보강
![Cycle 1](cards/cycle-01.svg)

#### #2 — Cli/Llm 순수 로직을 유닛테스트로 커버
![Cycle 2](cards/cycle-02.svg)

#### #3 — 그래프 뷰어: 활성 프로젝트 표시, 재시작 없이 전환
![Cycle 3](cards/cycle-03.svg)

### 2기 · 루프가 닫힌다

#### #4 — 폐루프: 기대평가 → 판정 → REINFORCES
![Cycle 4](cards/cycle-04.svg)

판정이 가능해진 첫 회차이고, 자기 자신을 **partial**로 판정했다. 이후 모든 회차는 작업 *전에* 검증 가능한
성공 기준을 갖는다.

### 3기 · 인증 확장 — 키 하나에서 진입 경로 다섯으로

#### #5 — 설계 먼저: API 키 단일 방식을 넘어선 LLM 인증
![Cycle 5](cards/cycle-05.svg)

#### #6 — 인증 추상화: AuthMode + IAuthProvider
![Cycle 6](cards/cycle-06.svg)

#### #7 — 키리스 오픈웨이트 E2E와 설정 병합 테스트
![Cycle 7](cards/cycle-07.svg)

#### #8 — GPT OAuth: 갱신·저장·device-code 폴링
![Cycle 8](cards/cycle-08.svg)

네 회차 중 셋이 `partial`, 그리고 `met`. 그 partial들은 실패가 아니었다. 각각이 "아직 증명 안 된 것"을
구체적으로 지목했고, 다음 회차가 그것을 증명했다.

### 4기 · 배포와 현지화

#### #9 — pdsa init: 스킬을 바이너리에 임베드
![Cycle 9](cards/cycle-09.svg)

#### #10 — 도움말·코칭·설정 전반의 i18n
![Cycle 10](cards/cycle-10.svg)

### 5기 · 셀프모델 프로바이더 — 에이전트가 자기 모델을 쓰게 한다

#### #11 — Codex OAuth 모드 (`~/.codex/auth.json` 재사용)
![Cycle 11](cards/cycle-11.svg)

#### #12 — 공식 셀프모델 연동 경로 조사
![Cycle 12](cards/cycle-12.svg)

#### #13 — 프로바이더 조사·설계 문서 작성
![Cycle 13](cards/cycle-13.svg)

#### #14 — `claude -p`를 LLM 프로바이더로 채택
![Cycle 14](cards/cycle-14.svg)

### 6기 · 실사용 결함 — 도그푸딩으로만 나오는 것들

#### #15 — 멀티프로젝트: 모든 명령에 `--project`
![Cycle 15](cards/cycle-15.svg)

#### #16 — 보강: #15가 실증하지 못한 갭을 닫는다
![Cycle 16](cards/cycle-16.svg)

#### #17 — AOT 단일 실행파일을 위한 인프로세스 뷰어
![Cycle 17](cards/cycle-17.svg)

#### #18 — Claude CLI 프로바이더의 타임아웃과 스피너
![Cycle 18](cards/cycle-18.svg)

### 7기 · 에이전트 친화 — 메모리가 복리로 쌓이기 시작한다

#### #19 — 구조화 `--json` 출력, recall, `--full`
![Cycle 19](cards/cycle-19.svg)

그래프가 보관소를 넘어 **입력**이 된 지점이다. `recall`이 과거 학습을 다음 `plan` 코칭에 주입하면서
루프가 같은 실수를 반복하지 않게 됐다.

### 8기 · 루프가 자기 자신을 감사한다

#### #20 — Akka.NET Streams와 주변 스택 조사
![Cycle 20](cards/cycle-20.svg)

#### #21 — 원자성, 제한적 재시도, 단계별 계측
![Cycle 21](cards/cycle-21.svg)

#### #22 — history / show — 루프 자신의 과거를 조회한다
![Cycle 22](cards/cycle-22.svg)

---

## 이 기록이 '루프 운영'에 대해 말해주는 것

**1. 기대 평가를 먼저 쓰는 것이 개선을 측정 가능하게 만든다.**
사이클 1–3은 실제 작업을 했지만 성공 여부를 판별할 방법을 남기지 않았다. 판정 필드가 "우리는 일을 했다"를
"18번 예측해서 11번 맞혔다"로 바꿔 놓았다.

**2. 쓸모 있는 판정은 `partial`이다.** 판정된 18회 중 7회가 깔끔한 성공이 아니었다(`partial` 6, `unmet` 1).
각각이 증명되지 않은 주장을 구체적으로 지목했고, 그중 셋(#4·#12·#15)은 조용히 잊히는 대신 명시적 보강
사이클로 닫혔다.

**3. 도그푸딩은 설계 리뷰가 놓치는 것을 찾는다.** #21의 고아 사이클 결함(일시적 네트워크 오류 하나가 장기
메모리를 오염시키는 경로)과 #22의 조회 명령 부재는 둘 다 코드를 읽어서가 아니라 도구를 *써서* 나왔다.

**4. 개별 수정보다 순서가 중요하다.** #21의 핵심 발견은 "재시도를 넣자"가 아니라 "원자성 없이 재시도를 넣지
말라"였다. 재시도는 실패 시도를 늘리는데, 실패 하나마다 고아 사이클이 남고 있었다.

**5. 측정 도구도 시스템의 일부다.** #21의 콜드스타트 비교는 존재하지 않는 45% 개선을 보여줬다. npm 셸 래퍼가
기준선에 ~190ms를 더하고 있었기 때문이다. 그 수치는 유지되지 않고 기록에서 수정됐다.

---

## 원본 기록 직접 읽기

여기 있는 내용은 믿어야 하는 요약이 아니다. 모든 회차를 직접 조회할 수 있다:

```bash
pdsa history --project akka-graph-loop                # 타임라인(오름차순)
pdsa history --from 15 --to 16 --full                 # 보강 한 쌍, 절삭 없이
pdsa show 21 --full                                   # 한 회차의 4단계·계측·링크
pdsa history --json | jq '.cycles[] | {id, verdict}'  # 기계 판독
pdsa view                                             # 그래프 자체를 브라우저로
```

카드는 같은 기록에서 [`cards/_generate.py`](cards/_generate.py)로 생성된다. 의도적으로 영문이며,
두 언어판이 정확히 같은 이미지를 공유한다.
