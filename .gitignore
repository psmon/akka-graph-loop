# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/

# IDE
.idea/
.vs/
*.user

# Secrets — 실제 시크릿(.secret/*.json)은 제외, *.tmp 템플릿은 커밋 허용
.secret/*.json
!.secret/*.json.tmp
secrets.json

# Kùzu native library cache (downloaded at build time)
native/_cache/

# Test/coverage
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
*.coverage
