<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>AkkaGraphLoop.Viewer</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Kùzu 인터롭(KuzuGraph)과 PDSA 리더를 재사용 -->
    <ProjectReference Include="..\AkkaGraphLoop.Samples\AkkaGraphLoop.Samples.csproj" />
  </ItemGroup>

  <!-- Kùzu 네이티브 라이브러리를 뷰어 출력 폴더에도 준비 -->
  <Import Project="..\..\native\Kuzu.targets" />

</Project>
